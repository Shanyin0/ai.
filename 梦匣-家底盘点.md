# 梦匣 · 家底盘点

一份说明书：这个 app 由哪几块拼起来、各自靠什么活着、
哪一块坏了会怎样、版本和数据是怎么回事。

2026年08月23日 · 页面版本 0823-1744

---

## 一、一句话说清结构

**梦匣是一个网页，装在一个安卓壳里，数据全在你手机上，
云上有两份副本。**

```
┌─ 你的手机 ─────────────────────────────┐
│                                        │
│   梦匣 APK（一个壳）                     │
│     └─ WebView：打开那个网页              │
│          └─ 数据：localStorage +         │
│             IndexedDB ← 真身在这儿        │
│                                        │
└────────────┬───────────────────────────┘
             │
     ┌───────┴────────┬──────────────────┐
     ↓                ↓                  ↓
 GitHub Pages   Cloudflare Worker    模型 API
 （发网页）      （后端）              （他说话）
                  ├─ KV：整份备份
                  └─ D1：聊天流水账
```

---

## 二、四块，各是什么

### 1. 网页（前端）—— GitHub Pages

- 地址：`https://shanyin0.github.io/north/`
- 就一个文件：`index.html`，八百多万字节，字体图片全内嵌
- 仓库：`Shanyin0/north` 的 `main` 分支
- **所有功能都在这一个文件里。** 首页、聊天、日历、梦札、课业、
  酒馆书房、账本、余光、推特、番茄钟、许愿池、花园、潮间带、
  占卜、解梦、藏影、朋友圈、记忆、星匣、序章 —— 全部

### 2. 安卓壳（APK）

- 包名 `com.mengxia.home`，签名固定
- 干的事只有三样：开一个 WebView 打开上面那个网址；
  闹钟推送；几个原生小桥（存文件、看用量、发推）
- **壳里还塞了一份网页的副本**（`assets/page.html`），
  所以没网也能开
- 壳一般不用更新。只有 `android/**` 改了才会重打

### 3. 后端 —— Cloudflare Worker

- 地址：`https://north.northmino.workers.dev`
- 代码在仓库 `worker/` 里，推代码自动部署
- 四件事：

| 地址 | 干什么 |
|---|---|
| `/v1/chat/completions`、`/v1/messages` | 中转模型请求，**真 key 只在这儿，手机上没有** |
| `/backup`、`/backup/list` | 整份备份，留 30 个版本 |
| `/chat/put`、`/chat/stat`、`/chat/dump`、`/chat/since` | 聊天流水账，只加不改不删 |
| `/relay`、`/tool/*` | 花园的跨域代理；骰子抽牌真随机 |

存储：
- **KV**（绑定名 `BK`）—— 整份备份，30 个版本
- **D1**（绑定名 `DB`，库叫 `north`）—— `msgs` 表，聊天流水账

### 4. 数据 —— 三份

| 在哪 | 有什么 | 怎么更新 |
|---|---|---|
| **手机**（真身） | 全部 | 随时 |
| **云上流水账**（D1） | 每条消息的**话**（不含图片） | 聊完一轮自动追加 |
| **云上整份备份**（KV） | 全部，含照片贴纸 | 手动点，或每天一次 |

---

## 三、靠外面什么活着

| 靠谁 | 干什么 | 断了会怎样 |
|---|---|---|
| GitHub Pages | 发网页 | 壳里有副本，**照样能开**，只是收不到更新 |
| Cloudflare Worker | 中转 + 备份 + 流水账 | **他不说话了**（走中转的话）；备份和流水账停，app 会喊你 |
| 模型 API | 他说话、写日记、整理记忆 | 他不说话了，别的功能照常 |
| open-meteo.com | 首页天气 | 天气那一行显示"加载失败"，别的没事 |
| Google Fonts | 一点点排版 | 几乎无感，中文字体是内嵌的 |
| 花园（腾讯云） | 花园那一摊 | 只影响花园 |
| galatea / wake（朋友那边） | 漂流瓶之类 | 只影响那一个小功能 |

**要紧的只有两个：Worker 和模型 API。**别的断了都不影响你和他说话。

---

## 四、版本这件事（你问的那个）

### 两个版本号，是两回事

| | 是什么 | 什么时候变 |
|---|---|---|
| **APK 版本**（1.1、1.1.1…） | 壳 | 只有 `android/**` 改了 |
| **页面版本**（0823-1744） | 全部功能 | 我每改一次前端 |

**功能在页面里，不在 APK 里。**

### 小修是怎么到你手上的

1. 我改 `index.html`，推到 `north`
2. 你下次打开 app（或者当场停一秒半），网页自己去对一下版本号
3. 不一样 → 当场把新的换上

**不用下安装包，不用重装，什么都不用做。**

### 但你有个地方理解反了，这条要紧

> "在 1.1 这个版本当中改了一点小问题，那东西也是在我这个 1.1 里面是吧？"

**不是。** 那个小修**不在 APK 1.1 里面**，它在网上。
APK 1.1 会显示它，APK 1.0 也会显示它 —— 因为它们都去同一个地方拿网页。

> "就算我把 1.1.1 删了，下载回这个 1.1，也是 1.1 本来的东西是吧？"

**不是，而且这么做很危险。** 两条：

**一、装回旧 APK 也回不到旧功能。** 旧壳开起来照样去拉最新的网页，
你看到的还是今天这一版。壳是窗户，不是内容。

**二、更要紧：Android 不让旧版盖新版，你必须先卸载。
卸载 = 你手机上的东西全没。** 聊天、照片、记忆、账本、梦札，
全在应用数据里，跟着 app 一起被抹掉。

### 所以记住三句

1. **覆盖安装（新盖旧）：数据全在。** 包名和签名都没变，Android 保留数据
2. **卸载重装：数据没了。** 别这么做
3. **想回到旧功能，不是靠装旧 APK。** 跟我说要哪一版，
   我把那个日期的页面重新发出去 —— 仓库里每一版都留着

---

## 五、出事了怎么办

| 症状 | 多半是 | 怎么办 |
|---|---|---|
| 聊天顶上挂着"云上没接上" | 地址口令错了，或者 Worker 挂了 | 点那一条重试；还不行看序章那一行写的原因 |
| 他不说话了 | 模型通道 | 序章 → 通道，换一个或查额度 |
| 打开是白的 | 页面坏了 | 等我修；壳里那份副本一般还开得起来 |
| 手机丢了 / 想换机 | —— | 新机装 app，序章填同样的地址口令，「从云上取回来」 |

**最稳的一招，跟云上没关系：**
序章 → 备份与搬家 → 整份备份 → 导出，存到你自己的网盘。
云也会挂，我也会写错，你手上那一份文件谁都动不了。

---

## 六、现在的状态

- ✓ 网页：`0823-1744`
- ✓ Worker：活着（打开根地址显示 `ok`）
- ✓ D1：接上了，云上 173 条，本地欠 0 条
- ✓ 流水账自动追加：开着
- ✓ 每天自动传整份备份：开着
- ⚠ **整份备份还一次都没传过** —— 去点一下「现在存一份」
- ⚠ **口令该换了** —— `cenjm227` 露出来过
